#include<iostream>
using namespace std;

void findmaxconsecutiveones(int[]nums) {
    int ans=0;
    int left=0;
    int sum=0;

    for(int right=0; right<nums.length(); right++) {
        //hwo to use sliding window
        //add elements nums[right] to the window

        //while condition are not met
        //do same thing as you do up that is logic to remove left form the window and left++

        //update the answer
}
int main() {
    
    }

}